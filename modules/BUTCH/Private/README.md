# Private Functions & Tools

This directory contains scripts that are **internal** to the `BUTCH` module or used purely for development/testing purposes (like `test.ps1`).

Functions defined in this directory are *not* automatically exported to the user's PowerShell session. They are meant to act as helper functions that the `Public` scripts can call under the hood.
